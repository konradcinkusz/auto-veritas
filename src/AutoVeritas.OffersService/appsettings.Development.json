{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "ConnectionStrings": {
    "_comment": "Local-development database only; the compose stack creates this role with this password. Real environments inject ConnectionStrings__DefaultConnection through the platform secret store.",
    "DefaultConnection": "Host=localhost;Port=5432;Database=offersdb;Username=autoveritas;Password=autoveritas-dev"
  },
  "Jwt": {
    "MetadataAddress": "http://localhost:8081/.well-known/openid-configuration",
    "Issuer": "https://auth.auto-veritas.local",
    "Audience": "auto-veritas"
  },
  "Cors": {
    "AllowedOrigins": [
      "http://localhost:3000"
    ]
  }
}
